- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.alacritty.yaml : alacritty.yml
    ~/.config/fish/config.fish : config.fish
    ~/.config/nvim : nvim
    ~/.local/share/nvim : nvim
    ~/.vim: .vim
    ~/.vimrc: .vimrc
    ~/.tmux.conf: tmux.conf
